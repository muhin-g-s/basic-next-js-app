/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ['api.slingacademy.com', 'img.freepik.com']
    },
    img:{
        domains: ['img.freepik.com']
    }
}

module.exports = nextConfig
